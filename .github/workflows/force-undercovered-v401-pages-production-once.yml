name: Archived undercovered v401 push release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by release-undercovered-v401-pages-on-issue.yml"
