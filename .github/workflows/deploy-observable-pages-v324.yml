name: Retired partial Pages publisher - observable Pages v324

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Complete site deployment is owned by deploy-complete-pages-with-ai-search.yml."
