name: Retired partial Pages publisher - password reset v9 hotfix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Complete site deployment is owned by deploy-complete-pages-with-ai-search.yml."
