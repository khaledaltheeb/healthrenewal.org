name: Generate addiction atlas v5

on:
  push:
    branches: [main]
    paths:
      - 'data/addiction-atlas/**'
      - 'scripts/publish_addiction_atlas_v2.py'
      - 'scripts/publish_addiction_atlas_v3.py'
      - 'scripts/normalize_addiction_atlas_aliases.py'
      - 'scripts/normalize_addiction_atlas_page_dates.py'
      - 'scripts/validate_addiction_atlas_v2.py'
      - 'scripts/validate_addiction_atlas_manifest.py'
      - 'scripts/validate_addiction_atlas_dates.py'
      - 'scripts/generate_addiction_atlas_sitemap.py'
      - 'addiction/substances/classes/**'
      - 'addiction/substances/learn/**'
      - '.github/workflows/generate-addiction-atlas-v2.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: generate-addiction-atlas-v5
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Compile generator, normalizers, sitemap builder and validators
        run: |
          python -m py_compile scripts/publish_addiction_atlas_v2.py scripts/publish_addiction_atlas_v3.py
          python -m py_compile scripts/normalize_addiction_atlas_aliases.py scripts/normalize_addiction_atlas_page_dates.py
          python -m py_compile scripts/generate_addiction_atlas_sitemap.py scripts/validate_addiction_atlas_v2.py scripts/validate_addiction_atlas_manifest.py scripts/validate_addiction_atlas_dates.py

      - name: Normalize registered alias arrays
        run: python scripts/normalize_addiction_atlas_aliases.py

      - name: Generate substance and comparison pages from registered waves
        run: python scripts/publish_addiction_atlas_v3.py .

      - name: Synchronize visible and structured modification dates
        run: python scripts/normalize_addiction_atlas_page_dates.py

      - name: Regenerate evidence-aware atlas sitemap
        run: python scripts/generate_addiction_atlas_sitemap.py

      - name: Validate generated publication
        run: |
          python scripts/validate_addiction_atlas_manifest.py
          python scripts/validate_addiction_atlas_dates.py
          python - <<'PY'
          import json
          from pathlib import Path
          from xml.etree import ElementTree as ET

          root = Path('.')
          report = json.loads((root/'api/addiction-atlas-v2.json').read_text(encoding='utf-8'))
          manifest = json.loads((root/'data/addiction-atlas/substance-waves.json').read_text(encoding='utf-8'))
          intents = json.loads((root/'data/addiction-atlas/comparison-intents-v2.json').read_text(encoding='utf-8'))

          expected_slugs = set()
          for route in manifest.get('waves') or []:
              payload = json.loads((root/route.lstrip('/')).read_text(encoding='utf-8'))
              for item in payload.get('substances', []):
                  slug = item['slug']
                  assert slug not in expected_slugs, f'duplicate substance slug: {slug}'
                  expected_slugs.add(slug)

          expected_comparisons = {
              item['slug'] for item in intents.get('comparisons', []) if item.get('indexable')
          }

          assert report['status'] == 'passed', report
          assert report['substances'] == len(expected_slugs), (report, len(expected_slugs))
          assert report['indexableComparisons'] == len(expected_comparisons), (report, len(expected_comparisons))

          missing_substance_pages = sorted(
              slug for slug in expected_slugs
              if not (root/'addiction/substances'/slug/'index.html').is_file()
          )
          missing_comparison_pages = sorted(
              slug for slug in expected_comparisons
              if not (root/'addiction/compare'/slug/'index.html').is_file()
          )
          assert not missing_substance_pages, missing_substance_pages
          assert not missing_comparison_pages, missing_comparison_pages

          class_pages = list((root/'addiction/substances/classes').glob('*/index.html'))
          learn_pages = list((root/'addiction/substances/learn').glob('*/index.html'))
          assert len(class_pages) >= 8, len(class_pages)
          assert len(learn_pages) >= 7, len(learn_pages)

          ns = {'sm': 'http://www.sitemaps.org/schemas/sitemap/0.9'}
          sitemap_root = ET.parse(root/'sitemap-addiction-atlas.xml').getroot()
          sitemap_urls = {node.text for node in sitemap_root.findall('sm:url/sm:loc', ns)}
          expected_substance_urls = {
              f'https://healthrenewal.org/addiction/substances/{slug}/' for slug in expected_slugs
          }
          expected_comparison_urls = {
              f'https://healthrenewal.org/addiction/compare/{slug}/' for slug in expected_comparisons
          }
          assert expected_substance_urls <= sitemap_urls, sorted(expected_substance_urls - sitemap_urls)
          assert expected_comparison_urls <= sitemap_urls, sorted(expected_comparison_urls - sitemap_urls)
          assert len(sitemap_urls) == 21 + len(expected_slugs) + len(expected_comparisons), len(sitemap_urls)

          print({
              'expectedSubstances': len(expected_slugs),
              'expectedComparisons': len(expected_comparisons),
              'sitemapUrls': len(sitemap_urls),
              'classPages': len(class_pages),
              'learnPages': len(learn_pages),
              'report': report,
          })
          PY

      - name: Commit normalized data and generated atlas publication
        run: |
          set -e
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/addiction-atlas addiction/substances addiction/compare api/addiction-atlas-v2.json sitemap-addiction-atlas.xml
          if git diff --cached --quiet; then
            echo 'No generated atlas changes to commit.'
            exit 0
          fi
          git commit -m 'build(addiction-atlas): publish registered evidence waves'
          git push origin HEAD:main
