name: Build clean complete site and deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compatibility-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Show current production workflow
        run: echo "Production deployment is handled by deploy-production-v3.yml."
