name: Validate global contrast guard

on:
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contrast-audit:
    runs-on: ubuntu-latest
    env:
      SITE_BASE: https://khaledaltheeb.github.io/pterminology-site/
    steps:
      - uses: actions/checkout@v4

      - name: Reconstruct current site generators
        run: |
          set -euxo pipefail
          mkdir -p /tmp/v10-content
          cp content/sectors-v10/family.json /tmp/v10-content/family.json
          cp content/sectors-v10/child.json /tmp/v10-content/child.json
          cp content/sectors-v10/home.json /tmp/v10-content/home.json
          cp content/sectors-v10/women.json /tmp/v10-content/women.json
          cp content/sectors-v10/tips.json /tmp/v10-content/tips.json
          cat .v10bundle/part00 .v10bundle/part01 .v10bundle/part02 .v10bundle/part03 .v10bundle/part04 | base64 --decode > /tmp/v10-source.tar.gz
          tar -xzf /tmp/v10-source.tar.gz
          cp /tmp/v10-content/*.json content/sectors-v10/
          cat .generator-v6/part00 .generator-v6/part01 .generator-v6/part02 .generator-v6/part03 > build_site.py
          python scripts/verify_enricher_v7.py
          python scripts/verify_expander_v9.py
          python -m py_compile build_site.py scripts/scale_site_v8.py scripts/design_content_v10.py scripts/apply_contrast_v11.py expand_site_v9.py
          node --check content/accessibility/hero-header-contrast-v283.js

      - name: Build and apply adaptive contrast globally
        run: |
          set -euxo pipefail
          python build_site.py
          python enrich_site_v7.py
          cp google644f1f7a8b7aaa2b.html _site/google644f1f7a8b7aaa2b.html
          python scripts/scale_site_v8.py
          python expand_site_v9.py
          python scripts/design_content_v10.py
          python scripts/apply_contrast_v11.py _site

      - name: Verify the reported page and adaptive contract
        run: |
          set -euo pipefail
          grep -q 'خبرة واسعة تشمل الرضا والمعنى والقدرة على إدارة الانفعالات وبناء علاقات داعمة.' _site/terms/psychological-well-being/index.html
          grep -q 'v11 — global readable contrast layer' _site/assets/css/theme-v10.css
          grep -q 'v11 — runtime contrast guard' _site/assets/js/app-v10.js
          grep -q 'v283 — adaptive hero/header contrast contract' _site/assets/css/hero-header-contrast-v283.css
          grep -q 'v283 — computed-style hero/header contrast guard' _site/assets/js/hero-header-contrast-v283.js
          test ! -e _site/assets/css/hero-header-black-v282.css
          python - <<'PY'
          import json
          from pathlib import Path

          root = Path('_site')
          report = json.loads((root / 'api/contrast-audit-v11.json').read_text(encoding='utf-8'))
          assert not report['failures'], report
          expected = report['content_pages']
          assert expected == report['pages_with_theme'], report
          assert expected == report['pages_with_base_guard'], report
          assert expected == report['pages_with_adaptive_hero_header_contract'], report
          assert report['pages_with_legacy_black_contract'] == 0, report
          assert report['target_page_found'] and report['target_sentence_found'], report
          assert report['hero_header_contract'].startswith('adaptive WCAG AA'), report
          assert expected >= 2800, report
          print(json.dumps(report, ensure_ascii=False, indent=2))
          PY

      - name: Upload contrast audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: contrast-audit-v11
          path: _site/api/contrast-audit-v11.json
          if-no-files-found: warn
          retention-days: 3
