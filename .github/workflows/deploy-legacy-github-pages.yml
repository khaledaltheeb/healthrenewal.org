name: Retired legacy GitHub Pages publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Production is served only by deploy-production-v3.yml on healthrenewal.org."
