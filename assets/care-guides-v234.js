(()=>{const q=document.querySelector('[data-care-search]');const buttons=[...document.querySelectorAll('[data-care-filter]')];const cards=[...document.querySelectorAll('[data-care-card]')];const count=document.querySelector('[data-care-count]');const empty=document.querySelector('[data-care-empty]');let active='all';const norm=s=>(s||'').toLocaleLowerCase('ar').normalize('NFKD').replace(/[\\u064B-\\u065F\\u0670]/g,'');function apply(){const term=norm(q?.value);let shown=0;cards.forEach(card=>{const matchCategory=active==='all'||card.dataset.category===active;const matchText=!term||norm(card.dataset.search).includes(term);card.hidden=!(matchCategory&&matchText);if(!card.hidden)shown++;});if(count)count.textContent=String(shown);if(empty)empty.hidden=shown!==0;}buttons.forEach(b=>b.addEventListener('click',()=>{active=b.dataset.careFilter;buttons.forEach(x=>x.setAttribute('aria-pressed',String(x===b)));apply();}));q?.addEventListener('input',apply);apply();})();
document.querySelectorAll('[data-category-jump]').forEach(a=>a.addEventListener('click',()=>{const b=document.querySelector(`[data-care-filter="${a.dataset.categoryJump}"]`);b?.click();}));
