# Accessibility governance

The public Arabic accessibility statement is in `index.html`; the machine-readable baseline is in `evaluation.json`. Both are validated by `scripts/validate_accessibility_governance.py` and the dedicated GitHub Actions workflow.
