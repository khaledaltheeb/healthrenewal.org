# تدقيق تغطية خرائط الموقع — 2026-08-04

## مصدر القياس

تقرير Artifact الناتج من بوابة `Validate sitemap engine and production hooks` على الرأس `9462a0dc0ef0b60242d404d5db8efd23533c4079`.

## النتيجة المرصودة

- `indexable_pages`: 212
- `primary_sitemap_urls`: 212
- خرائط العائلات المولدة: 5
- التوزيع:
  - `main`: 97
  - `magazine`: 80
  - `provider-platform`: 25
  - `special-needs`: 8
  - `learning-paths`: 2
- `skipped_noindex`: 9
- `skipped_verification`: 2
- `preserved_custom_domain_sitemaps`: 0

## الخطر

المولد يعلن نجاح التوليد، لكن العدد النهائي لا يثبت تغطية مخزون المنصة المنشور. يجب ألا تعتبر بوابة النشر التقرير ناجحًا لمجرد أن ملفات XML صالحة نحويًا؛ يلزم عقد اكتمال يقارن الصفحات القابلة للفهرسة في حزمة الإنتاج بما أدرج فعليًا في الخرائط، ويشرح كل استبعاد.

## عقد الإصلاح المقترح

1. بناء قائمة مرجعية من كل ملفات `index.html` داخل Artifact الإنتاجي بعد استبعاد المسارات غير العامة والموسومة `noindex` وملفات التحقق.
2. مقارنة المجموعة المرجعية باتحاد عناوين جميع خرائط الموقع المعلنة في `sitemap-index.xml`.
3. إخراج القوائم التالية في تقرير JSON:
   - `indexable_html_pages`
   - `sitemap_urls`
   - `missing_from_sitemaps`
   - `orphan_sitemap_urls`
   - `excluded_with_reason`
4. الفشل المغلق عند وجود صفحة قابلة للفهرسة غير مدرجة، أو URL في الخريطة بلا ملف إنتاجي مطابق.
5. عدم استخدام ادعاءات أعداد الصفحة الرئيسية بديلًا عن جرد Artifact الفعلي.
6. تشغيل العقد على `main` وعلى PRs التي تغير الناشر أو الخرائط أو `robots.txt`.

هذا المستند تشخيص تقني فقط ولا يغير محتوى طبيًا أو ادعاءات علمية أو ملفات Sitemap.