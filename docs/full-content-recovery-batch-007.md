# استعادة المحتوى — الدفعة 007

تاريخ التدقيق: 2026-08-06

## خط الأساس

- الفرع: `agent/full-content-recovery-v3`
- مبني مباشرة من `main` عند الالتزام `1c376e44c315c2f51456c2dc91493d72a30f5249`.
- PR #1080 دُمج بالفعل في `main` عند Merge SHA `1bebbb3e4ac461309741f5ce8682f4e140544f0d`، لذلك لا يجوز مواصلة العمل على فرعه القديم أو إعادة فتحه.
- الصفحات المستعادة سابقًا أصبحت جزءًا من خط الأساس الحالي:
  - `special-needs/guides/communication/index.html`
  - `learning-paths/all-pages/index.html`
  - `special-needs/all-pages/index.html`

## الفروع والنسخ التي تمت مقارنتها

### `agent/content-expansion-100-v2`

حالة المقارنة مع `main`: متباعد، يتقدم تاريخيًا بـ7 التزامات ويتأخر بـ343 التزامًا.

ظهرت عائلات صفحات مفقودة من `main` تحت:

- `care-guides/evidence-guided/**`
- `comparisons/disability-support/**`
- `daily-tools/disability-support/**`
- `learning-paths/evidence-guided/**`

تم فحص نموذج كامل من هذه العائلة:

- `care-guides/evidence-guided/caregiver-burnout-prevention/index.html`
- Git blob: `aabf2e08c6f376b1e2335a8d31b76a390f0ef8d6`

### قرار الاستبعاد المؤقت

لم تُستعد الصفحة. رغم طولها ووجود RTL وcanonical وArticle Schema ومراجع رسمية، فإن متنها يعتمد بصورة واضحة على قالب مولد متكرر، ويكرر الفقرات العامة نفسها داخل خطوات متعددة مع تغيير العنوان فقط. الحجم هنا لا يثبت غنى علميًا فريدًا، واستعادتها كما هي ستعيد محتوى آليًا متشابهًا قد يضعف الجودة وSEO ولا يحقق معيار النسخة الأغنى علميًا.

لا يعني الاستبعاد حذف المصدر التاريخي؛ يبقى الفرع محفوظًا، وتبقى بياناته مرشحًا لاستخراج المعلومات الفريدة يدويًا عند مقارنتها بصفحات حالية ذات صلة.

### `agent/addiction-reference-center-v1`

حالة المقارنة مع `main`: متباعد، يتقدم تاريخيًا بـ40 التزامًا ويتأخر بـ339 التزامًا.

تم فحص:

- `addiction/family-guide/index.html`
- Git blob التاريخي: `b2e298a87895f9c01b19d97beadd08cf70a84059`

النسخة الحالية في `main` تحتفظ بجميع الأقسام العلمية والعملية الجوهرية الموجودة في النسخة التاريخية، وتضيف غلاف منصة روافد، وGTM، وOpen Graph، وTwitter metadata، والأيقونات، والترخيص، وتطبيع غلاف المنصة. لذلك النسخة الحالية أطول وأكمل بنيويًا، ولا توجد معلومة فريدة مثبتة تستوجب استبدالها أو دمجًا جديدًا.

## فحص الصفحات التي تبدو مختصرة

تم البحث عن إشارات مثل `قريبًا` في الصفحات الحالية. ظهرت نتائج متعددة، لكن الفحص المباشر لنموذج `family-guide/conditions/rett-syndrome/index.html` أثبت أن الصفحة ليست قالبًا فارغًا: تحتوي على وصف طبي، علامات، خطوات أولى، محاذير، طوارئ، أسئلة للمختص، FAQ Schema، RTL، والطباعة. لذلك لا يجوز اعتبار وجود كلمة منفردة دليلًا كافيًا على فقد المحتوى.

## النتيجة

- لم يُحذف أو يُختصر أي محتوى.
- لم تُستبدل صفحة حالية بنسخة أقدم أو مولدة أضعف.
- لم تُمس ملفات PR #1076 المحجوزة لمسار `learning-paths/self-advocacy/**`.
- لم تُنفذ أي تغييرات إنتاجية في هذه الدفعة؛ التغيير الوحيد هو سجل التدقيق هذا.

## معيار الدفعة التالية

يُقبل مرشح الاستعادة فقط إذا تحقق واحد أو أكثر مما يلي مع دليل قابل للتوثيق:

1. المسار مفقود كليًا من `main` وله نسخة تاريخية أصلية غير قالبية.
2. الصفحة الحالية أقصر ماديًا وعلميًا من نسخة تاريخية، مع وجود فقرات أو جداول أو مصادر فريدة قابلة للدمج.
3. الناشر أو المولد الحالي استبدل محتوى يدويًا غنيًا بقالب خط أساس أو محتوى مكرر.
4. يمكن ربط الصفحة بمصدر الحقيقة والناشر وSitemap والبحث وAPI وArtifact الإنتاج، وليس نقل HTML معزولًا فقط.

Tracks #158.
