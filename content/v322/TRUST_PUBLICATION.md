The v322 production wrapper must copy the institutional `trust/index.html` source into the generated site before integrity checks, register `/trust/` in the library sitemap, and expose its visible-word and publication status in the v322 API report.
