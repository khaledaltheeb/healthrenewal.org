(()=>{'use strict';const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const grades=['','0','1','1+','2','3','4'];let nextRow=1;function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function rowHtml(i){return `<tr data-row="${i}"><td>${i}</td><td><select aria-label="الجهة للقياس ${i}"><option value="">—</option><option>يمين</option><option>يسار</option><option>ثنائي</option></select></td><td><input class="wide" aria-label="العضلة أو المجموعة للقياس ${i}" placeholder="مثال: مثنيات المرفق"></td><td><input class="wide" aria-label="المفصل أو الحركة للقياس ${i}" placeholder="مثال: مد المرفق"></td><td><input class="wide" aria-label="وضع المريض للقياس ${i}" placeholder="استلقاء/جلوس..."></td><td><input class="wide" aria-label="بداية ونهاية الحركة للقياس ${i}" placeholder="مقصر → ممدد"></td><td><select class="mas-grade" aria-label="درجة MAS للقياس ${i}">${grades.map(g=>`<option value="${esc(g)}">${g||'—'}</option>`).join('')}</select></td><td><input aria-label="الألم أو التيبس للقياس ${i}" placeholder="لا/نعم + وصف"></td><td><input class="wide" aria-label="ملاحظات القياس ${i}"></td></tr>`}function addRow(){const body=$('#mas-rows');body.insertAdjacentHTML('beforeend',rowHtml(nextRow++));wire();summarize()}function wire(){$$('#mas-rows input,#mas-rows select').forEach(el=>{if(el.dataset.wired)return;el.dataset.wired='1';el.addEventListener('input',summarize);el.addEventListener('change',summarize)})}function summarize(){const vals=$$('.mas-grade').map(x=>x.value).filter(Boolean);$('#mas-summary').textContent=`${vals.length} قياسات`;if(!vals.length){$('#mas-distribution').textContent='لا توجد درجات مسجلة بعد.';return}const counts={};vals.forEach(g=>counts[g]=(counts[g]||0)+1);$('#mas-distribution').textContent=['0','1','1+','2','3','4'].filter(g=>counts[g]).map(g=>`${g}: ${counts[g]}`).join(' · ');$('#mas-live').textContent=`تم تسجيل ${vals.length} درجات MAS دون حساب مجموع كلي.`}function clear(){['mas-name','mas-date','mas-rater','mas-context','mas-protocol','mas-notes'].forEach(id=>{const el=document.getElementById(id);if(el)el.value=''});$('#mas-rows').innerHTML='';nextRow=1;for(let i=0;i<8;i++)addRow();summarize();$('#mas-live').textContent='تم مسح سجل MAS محليًا.'}$('#mas-add').addEventListener('click',addRow);$('#mas-clear').addEventListener('click',clear);$('#mas-print').addEventListener('click',()=>window.print());for(let i=0;i<8;i++)addRow();})();